import styled from 'styled-components';

export const Wrapper = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;

  padding: 10px;
  text-align: center;

  background-color: rgba(0, 0, 0, 0.5);

  color: #fff;
  font-weight: 300;

  a {
    color: inherit;
    font-weight: bold;
    margin: 0 7px;
  }
`